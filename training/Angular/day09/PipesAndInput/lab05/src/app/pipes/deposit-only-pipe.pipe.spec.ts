import { DepositOnlyPipePipe } from './deposit-only-pipe.pipe';

describe('DepositOnlyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DepositOnlyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
